pipeline {
    agent any

    stages {
        stage('git') {
            steps {
                echo 'git'
                sh 'pwd'
                sh 'ls - la'
                git branch: 'main', credentialsId: 'GitHub_SSH_key', url: 'git@github.com:Salvadory/WebSite.git'
                
            }
        }
        
        stage('Build') {
            steps {
                echo 'Building'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing'
            }
        }
    

        stage('Deploy') {
            steps {
                echo 'Deploying'
            }
        }
    }
}
